# Showcase (v7) - append-only decision log

One line per decision, with the number that drove it. Never rewrite an earlier line.

- 2026-09-12 Final session opened. Read docs/handover/v7_final_session.txt, README.md, evidence/cable_routing_v6.json (verdicts first) and artifacts/cell/block.json. No refitting, no new study: the four blocks are closed.
- 2026-09-12 Picked the matched triple the replay needs before writing any render code: `RC1_l15_compliant4000_m0_{E0}_{filtered,conservative,unfiltered}_r0`. Same cell, same service loop, same mount, same estimate; filtered issues action 9 (100.6 mm) and loses c3 at t=6.196 s, unfiltered issues action 4 (106.0 mm) and loses c3 at t=5.850 s, conservative issues action 0 (6.0 mm) five times and seats with all five clips. Its C1 headroom series is 0.0699 / 0.0666 / 0.0633 / 0.0600 / 0.0567 m, which is the series the handover names, so this is the request the budget clip must draw.
- 2026-09-12 Found the tree BROKEN on arrival, not by this session: commit 1b857e6 deleted scripts/probe_cable_robot.py while scripts/evaluate_cable_recovery_v2.py and scripts/evaluate_cable_perception_v4.py still imported `render` from it, so every simulator entry point raised ModuleNotFoundError at import. 250 unit tests passed throughout, because none of them import the evaluators. Moved the 15-line `render` helper into evaluate_cable_recovery_v2.py, which both already share. Nothing physical changed; 250 tests still pass.
- 2026-09-12 Gave scripts/evaluate_cable_routing_v6.py an optional `observer` callback instead of copying its loop into the renderer. The handover's named trap is a capture path that draws a different number of random numbers than the block; with one loop and one perception stream that cannot happen by construction, and `reproduces_block` still checks it.
- 2026-09-12 STAGE A done. Three clips, all decoded back off disk and checked: triptych 329 frames at 1920x900, estimate 352 at 1280x720, budget 329 at 1280x720, 30 fps, 0 blank sampled frames of 27 measured, and reproduces_block TRUE for all five captures. artifacts/showcase/video.json.
- 2026-09-12 Camera chosen by looking, not by guessing: rendered five candidate set-ups from an already-captured frame and picked distance 0.33 m, azimuth 138 deg, elevation -18 deg, which is the only one of the five that holds the plug, the ridge and the clamp without the unlit face of the mount riser filling a third of the frame.
- 2026-09-12 Added a bead over each clip, green while the cable is through it. Without it a viewer reads "4 of 5 clips held" as a caption rather than as a thing that happened to c3, the clip at the top of the ridge, which is where 1,017 of the block's 1,144 losses happened.
- 2026-09-12 Wording fix, because it would have been an overclaim: the unfiltered arm's spent fraction is labelled "the budget the check WOULD HAVE reported". The worker records a budget for every issued motion, but that arm never consults the filter.
- 2026-09-12 STAGE B done. scripts/build_showcase.py reads 10 committed records and emits both artifacts/showcase/page.json and the page; nothing on the page is typed by hand. Published to https://claude.ai/code/artifact/71352222-82b3-4676-901c-c2c4852eea3c with 8 files, read back after publishing, renders.
- 2026-09-12 Chart colours were computed, not chosen by eye: the dataviz validator failed the first teal on chroma and failed amber-against-red outright (normal-vision delta E 11.4, below the 15 floor). Settled on #00937C / #C02D1B in light and #2FA98F / #E0584F in dark, all six checks passing in both modes, and the clip-amber accent is kept for rules and labels only so it never sits beside the red as a data colour.
- 2026-09-12 The outcome figure shows COUNTS over the whole denominator, not rates, with the censored routes drawn as a hatched middle segment. A rate would have hidden that at E4 half of every arm's routes were cut short before they could break anything, which is the thing the censoring rule exists to keep visible.
- 2026-09-12 Looked at the three generated figures before publishing and fixed what looking showed: the clip-height labels collided with the axis caption in the elevation, the pilot ladder had no legend, and white segment labels on the hatched fill were unreadable.
- 2026-09-12 STAGE C done. scripts/workbench.py over WorkbenchSession; 13 new headless tests (263 total, 2.4 s) and a --self-check that passes all four of its checks in 36.5 s: the request it expands is field-for-field the registered one, re-screening the unmutated cell reproduces evidence/cable_cell_screen_v6.json to the digit on all four of its numbers, re-running the route reproduces the committed result including the C1 headroom series 0.069859494 / 0.066589254 / 0.063310336 / 0.060010847 / 0.056690161, and a moved clip is refused until it has been screened again.
- 2026-09-12 Caught a seeding bug before it could lie: narrowing the screen record to one group made the expansion restart its seed counter, so every group but the first would have been given the wrong seeds and the tool would have quietly shown a DIFFERENT request under a registered name. An unmutated session now hands the launcher the committed screen whole.
- 2026-09-12 The demonstration mutation - c3 raised from 6 mm to 9 mm - passes the re-run screen with all five clips retained, installed bend radius 47.7 mm against 50.1 mm and worst lateral clearance 0.52 mm against 0.45 mm. So it is a cell worth routing, not a cell the cable falls out of, which is what makes it a usable example.
- 2026-09-12 THE INTERACTIVE WINDOW WAS NEVER EXECUTED. This machine has no display. Everything the viewer wraps is headless and tested; launch_passive itself is one call in scripts/workbench.py view_observer() and is the single untested line in the tool.
- 2026-09-12 STAGE D done, in its own .deps/usd-venv so the cable environment's recorded package set does not drift. 200 recorded states written to one 5.4 MB .usd stage: opens, 286 prims, 70 meshes, 72 of 73 transforms animated over time.
- 2026-09-12 Two traps in the USD export, both paid for here. First, 36 geom names are not legal USD path segments - dots and slashes from the imported robot description, minus signs from the fixture's own coordinate-tagged names - and OpenUSD throws from four frames below anything you wrote. The export runs off a sanitised COPY of the scene written beside the original, so the asset paths still resolve and the measured file is untouched.
- 2026-09-12 Second, and worse because it exits zero: STEPPING the compiled scene at export time collapses the arm under gravity in 4.3 ms, and the first attempt wrote 200 frames of a fall. The export now REPLAYS the recorded rollout instead, so the stage carries the route the block scored. The handover's remembered "200 stepped frames" is the thing not to repeat.
- 2026-09-12 Closed the project. README gained the workbench section and a table of what now exists with its URL and command; ROADMAP became a record with the two near-misses written into it; AGENTS matched to the final file map, the three interpreters and the full command sequence; evidence/INDEX.json regenerated over 31 records. 263 tests pass on a clean tree.
- 2026-09-12 Rewrote the page after the owner said it was unreadable, and they were right: it used E0/E2/E4 and C1/C2/C3 without ever saying what they mean, and led with measurement machinery instead of the problem. Now: the problem, the three things that can break with their real limits (40 mm bend radius on a 4 mm cable, 0.30 N against the cable's own 0.49 N weight), one comparison table of five checks, then the findings. The error levels get their own table in millimetres with the study's label beside them. Every denominator, guard and margin moved into one collapsed section at the bottom.
- 2026-09-12 Gave the workbench --add-clip and --remove-clip, which is what makes it a routing tool rather than a clip nudger. Both rebuild the install waypoints, and the rebuild rule is checked against the registered route: it reproduces all five registered waypoints exactly, so a clip added by the tool is threaded the same way the measured ones were.
- 2026-09-12 The tool immediately earned its keep on three changes and answered no to all three, in six seconds each: a sixth clip at 212 mm does not settle (settle_instability, and the cable is only 460 mm), removing the ridge clip loses all four survivors, and flattening the ridge from 6 mm to 3 mm fails on cable motion at the settle deadline. That IS the answer an engineer wants before cutting metal.
- 2026-09-12 Re-rendered one route in Isaac Sim 5.1 RTX, 198 frames at 1920x1080, from the USD stage. Three things had to be got past: sphere lights render as white balls in shot and none of the documented ways to hide one worked, so the fills are distant lights; the last capture lands past the stage's final time sample and comes back blank; and driving the stage by setting the time by hand with the timeline paused looked right but dropped the scene out of frame after 40 per cent of every run - letting Replicator advance the timeline itself fixed it, 197 of 198 frames non-blank.
- 2026-09-12 Rewrote README and ROADMAP for someone who has never seen the project: the problem first, every term explained where it first appears, one comparison table, findings, then how to run it. The three failure modes now carry their real limits (40 mm bend radius on a 4 mm cable, 0.30 N against the cable's own 0.49 N weight) instead of being called C1, C2 and C3.
- 2026-09-12 Caught the Isaac record never being written: Isaac's shutdown takes the process with it, so anything after app.close() does not run. The first full render finished, exited zero and left no artifacts/showcase/isaac.json at all. The repository-contract test is what found it, because README linked to the missing file.
