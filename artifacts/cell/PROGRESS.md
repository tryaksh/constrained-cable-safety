# Routing cell (v6) - append-only decision log

One line per decision, with the number that drove it. Never rewrite an earlier line.

- 2026-09-12 Session opened. Read v6 handover, README, AGENTS, ROADMAP, evidence/cell_toolchain_v6.json. Baseline `pytest` 221 passed in 2.36 s on a clean tree at 8e654c2.
- 2026-09-12 Measured the base cell before drawing anything: settled cable occupies along 0.063-0.242 m, across -0.008-0.000 m on the shelf; routed guide 0.4540 m against a 0.46 m cable. A five-clip route therefore has ~180 mm of shelf run to work with, not an open board.
- 2026-09-12 Route-length arithmetic on candidate multi-clip waypoint sets (no physics): five straight clips reach the anchor at along 0.234; a corner to across -0.045 fits with the anchor at along 0.20; across -0.06 does not fit the 0.46 m cable. The cable is NOT lengthened - the filter's C1 threshold is a boot-to-anchor distance fitted on this exact cable, so changing rest length would silently invalidate it.
- 2026-09-12 Stage order deviation, deliberate: Stage 2's multi-clip code is written BEFORE Stage 1's CAD, because the CAD script is parametric in the cell config and the layout it draws has to be one that physically settles. Authoring first would mean drawing a jig the screen then rejects. CAD is re-run from the config, so the order costs nothing.
- 2026-09-12 Stage 2 code: `clips` is a list, `clip` reads as a one-element list, waypoints take an optional height, clips take `up_m` (with a pedestal) and `bearing_rad`. Reproduction check on two registered contexts (L1_l6 and the corner layout L10_l4): compiled scene XML SHA-256 IDENTICAL and settled centreline agreement 0.0 m exactly, against the 1e-6 m tolerance the handover sets. The refactor did not change the task.
- 2026-09-12 Pilot sweep, 132 unregistered cells over four geometry families, settled to find what installs. Three measurements came out of it: the service-loop surplus must be parked AFTER the last clip (a 6 mm surplus bowed into the 250 mm descent leg is a 28 mm lateral excursion that drags the cable out of every clip near the landing); the first clip cannot sit at the landing point (a clip at along 80 mm is lost in almost every cell); 22 mm clip pitch installs where 18 mm does not.
- 2026-09-12 Registered screen: 28 cells, 9 accepted, 19 rejected (68% rejection, against the ~33% the handover expected). RC1 - five clips, 22 mm pitch, a 6 mm ridge and a corner - survives at 6 of 7 service loops against RC2's 2, RC3's 1 and RC4's 0, and wins on the selection rule declared before the screen ran. Worst installed lateral clearance 0.41-0.53 mm, inside the 0.14-1.87 mm band the v4 reference layout installs at.
- 2026-09-12 CAD authored and exported: 8 parts, all watertight, all a single solid after the fuse, 3704 facets, lead-ins filleted on all five clips. Clips at three heights (0 / 3 / 6 mm) and four bearings (0 / -8.4 / -24.0 / -28.9 deg).
- 2026-09-12 Gotcha worth passing on: `freecadcmd script.py` runs the file under a module name of its own, so an `if __name__ == "__main__"` guard never fires - the script exits 0 having done nothing, silently. Cost twenty minutes.
- 2026-09-12 CAD is physics-neutral, measured not assumed: settled centreline agreement 0.0 m at three service loops, with and without the eight mesh geoms attached. The meshes are contype=0 conaffinity=0 group=2 mass=0 (TRAP 4).
- 2026-09-12 Pilot, 63 requests, 376 worker-seconds. Measured the two numbers the block turns on. The cell ADMITS a commanded retreat of about 18 mm: at 4/8/12/18 mm a route completes all five steps with all five clips kept and the connector seated; at 25 mm a clip goes at step 5, at 35 mm at step 3, at 50 mm at step 1. The shipped filter PERMITS 58 mm of headroom on this cell and approves eleven of the twelve registered candidate motions - it refuses only the largest, then issues one that spends 82 per cent of its own reported headroom and loses a clip. That 3x gap between what the check permits and what the route admits is the block.
- 2026-09-12 Did NOT rescale the action set and did NOT refit the filter. Either would answer a different question. The twelve registered actions are the perception study's, unchanged, so the routing numbers stay comparable with v4 and v5.
- 2026-09-12 The three arms are measurably distinct from the same state: unfiltered 0.106 m, filtered 0.1006 m (the filter refuses the largest), conservative 0.006 m. Verified on the pilot, not assumed.
- 2026-09-12 Registered a five-point mounting-offset ladder, because at E0 the estimate IS the truth and a second perception draw would be a byte-identical repeat - the composition study took four there and its E0 resolution was optimistic by that factor. The offsets flip E0 outcomes within a single group, measured. Repeats are therefore 1 at E0 and 6 at E2/E4.
- 2026-09-12 Contract frozen: 2,340 routes, 11,700 decisions, 60 physical contexts, per-step resolution 0.0167 at E0 and 0.0028 at E2/E4 against a 0.05 margin. The runner checks the resolution level by level so a well-sized E4 cannot hide an unresolvable E0.
- 2026-09-12 Block LAUNCHED as run routing-v6-s1 at 18:23:40 UTC on 20 workers, from a clean tree at 26596c8 with the contract already committed. Measured rate 52 routes in the first 2.5 minutes, so about 110 minutes for 2,340 routes against the 290-minute cap. One shard.
- 2026-09-12 Wrote scripts/fit_routing_v6.py so the block can be scored by whoever is here when it lands. It takes --partial and says so in the record if the block did not finish.
- 2026-09-12 Rendered the cell to six stills - three views, each with the CAD meshes and with primitives only. Both variants settle with all five clips retained; the CAD one carries 152 geoms against 144. evidence/cable_cell_v6_*.png.
- 2026-09-12 Block FINISHED in 87 minutes: 2,340 of 2,340 routes, 11,700 decisions offered, 4,414 motions issued, 266.8 million integration steps, 28.7 summed worker-hours, ZERO privilege-guard and ZERO mutation-guard events, zero settling rejections.
- 2026-09-12 Fitted. 1 of 3 registered predictions correct. P1 FALSE: the filter does not earn its place over a five-clip route - filtered and unfiltered are the same supervisor here (clip-loss gap 0.000 / +0.025 / -0.008, none beyond the 0.05 margin, and both issue exactly one motion per route and lose a clip on it). P2 FALSE: the per-step clip rate does NOT keep rising - on the arm that reaches step 5 it FALLS (E2 0.314 to 0.000, E4 0.181 to 0.090). The risk is front-loaded at the first motion, not cumulative. P3 CORRECT including its predicted collapse: appetite beats representation at E0 (+0.333) and E2 (+0.211), and collapses inside the margin at E4 (+0.025). That promotes the composition study's post-hoc appetite finding to a certified one.
- 2026-09-12 The clip that goes is c3, the raised one at the top of the ridge: 1,017 of 1,144 losses, against 127 for c1 and none at all for c2, c4 or c5. The cable lifts out at the highest point of the route first.
